[project]
name = "zero-rl-dataset"
version = "0.1.0"
description = "RL dataset + native-toolchain graders for editing zerolang projects (Prime Intellect / Laguna XS.2)."
requires-python = ">=3.10"
# Tools are stdlib-only; the Prime env package declares its own deps.
